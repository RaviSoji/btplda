class FeatureTeacher:
    def __init__(self, u_model, target_model)
        assert len(u_model.shape) == 1

        self.prior_cov = target_model.prior_params['cov_diag']

    def teach(

    def prior

    def likelihood
